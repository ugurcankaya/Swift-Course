//Functions

func add(firstNumber:Int, to secondNumber:Int)->Int{ //to is used as label, label will be used by user but in functions parameterName is.
    return firstNumber+secondNumber
}

add(firstNumber: 5, to: 5)
